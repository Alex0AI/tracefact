# TraceFact v0.1.2

The CLI now reads its version from package metadata, and the test suite launches the real CLI process to analyze the bundled Codex fixture, inspect exported reports, and verify the Replay Capsule.

This release includes all v0.1.1 recovery-status and reproducible demo improvements.
