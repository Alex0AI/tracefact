# TraceFact v0.1.1

This polish release makes recovered runs report their final observed outcome, keeps generated dataset snapshots byte-stable in CI, and adds a reproducible terminal demo GIF built from the real offline fixture.

All v0.1.0 capabilities remain unchanged. No API key, model, Docker image, or network connection is required for the demo.
