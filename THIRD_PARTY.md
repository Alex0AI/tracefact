# Third-party notices

TraceFact's runtime core has no production npm dependency. Development dependencies are locked in `package-lock.json` and used for TypeScript, Vite, ESLint, Prettier, and Vitest. Their licenses are not relicensed by TraceFact.

GitHub Actions referenced by workflow version tags: `actions/checkout`, `actions/setup-node`, `actions/upload-artifact`, `actions/configure-pages`, `actions/upload-pages-artifact`, `actions/deploy-pages`, and `github/codeql-action`. Their respective upstream repositories and licenses govern them.

Adapter names are nominative references to compatible projects. TraceFact is not affiliated with OpenAI, Google, Browser Use, Anomaly, Vercel, OpenHands, LangChain, Langfuse, Arize, or AgentOps. No upstream brand assets, README text, UI, data, or implementation are copied.

The dashboard requests Manrope and DM Mono from Google Fonts when online; system fallbacks preserve offline function. A future release should self-host OFL font subsets for fully offline visual parity.
